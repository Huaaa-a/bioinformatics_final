# Checklist

- [x] `requirements.txt` 存在并包含 `biopython`、`openpyxl`、`pandas`、`python-dotenv`
- [x] `scripts/fetch_pubmed_test_set.py` 实现并能成功执行
- [x] 脚本能正确读取 xlsx 的 24 个受体,无空字段
- [x] **per-receptor 查询**(不是 per-system),每次 esearch 对应 1 个基因
- [x] `query_receptor_gene` 字段等于搜索词(由构造保证,不是 round-robin)
- [x] 剥离 HTML 标签(`<sub>` 等)避免打断匹配
- [x] 扫描 `mentioned_receptors_in_abstract`(24 个基因)+ `mentioned_receptor_names`(全名 + 短别名如 "5-HT2A"、"H2 receptor")
- [x] `low_confidence_query` 标记正确(仅 1/86)
- [x] 跨受体 PMID 合并 mentioned_*,不重复入库
- [x] 限速正确(无 API key 时 0.34s/次)
- [x] 缺 `ENTREZ_EMAIL` 时脚本退出并打印明确错误
- [x] 缺 `ENTREZ_API_KEY` 时仅打印提示,继续运行
- [x] `data/pubmed_test_set.json` 结构符合 spec(14 个字段)
- [x] `data/pubmed_test_set_summary.csv` 24 行(每受体 1 行)
- [x] 重复运行脚本不会产生重复 PMID(日志出现 `Skipped N already-fetched PMIDs`)
- [x] 7 个神经递质系统均有命中
- [x] 14 个目标抽取字段已记录到 spec.md 供下一阶段 LLM 抽取使用
- [x] `data/_check_assignments.py` 审计脚本存在,错配率 ≤ 5%
- [x] 仓库已推送到 GitHub
